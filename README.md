# fynkus-reservation-mvp
MVP for a community space booking system. Admins can view availability and create reservations for shared amenities. Built with Symfony (Hexagonal Architecture, TDD) and Vue 3 + TypeScript in a Dockerized monorepo. Easily runnable via docker-compose up --build.
